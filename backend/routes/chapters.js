
const express = require('express');
const router = express.Router();

// Placeholder routes for chapters
router.get('/', (req, res) => {
  res.json({ message: 'Chapters routes - to be implemented' });
});

module.exports = router;
