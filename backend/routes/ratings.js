
const express = require('express');
const router = express.Router();

// Placeholder routes for ratings
router.get('/', (req, res) => {
  res.json({ message: 'Ratings routes - to be implemented' });
});

module.exports = router;
