
const express = require('express');
const router = express.Router();

// Placeholder routes for collections
router.get('/', (req, res) => {
  res.json({ message: 'Collections routes - to be implemented' });
});

module.exports = router;
