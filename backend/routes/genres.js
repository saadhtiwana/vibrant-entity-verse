
const express = require('express');
const router = express.Router();

// Placeholder routes for genres
router.get('/', (req, res) => {
  res.json({ message: 'Genres routes - to be implemented' });
});

module.exports = router;
