
const express = require('express');
const router = express.Router();

// Placeholder routes for collaborations
router.get('/', (req, res) => {
  res.json({ message: 'Collaborations routes - to be implemented' });
});

module.exports = router;
