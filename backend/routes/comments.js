
const express = require('express');
const router = express.Router();

// Placeholder routes for comments
router.get('/', (req, res) => {
  res.json({ message: 'Comments routes - to be implemented' });
});

module.exports = router;
